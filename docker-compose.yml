version: '2'

services:
    web:
        build:
            context: .
            dockerfile: node.dockerfile
        image: modern-web
        volumes:
            - "./app:/modern-web/app"
        ports:
            - "8888:8888"
            - "3001:3001"